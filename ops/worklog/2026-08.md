# 業務台帳 2026-08

## 2026-08-02
- 08:15 SEO強化を実装（未コミット・Masaru承認待ち）: ①ops/check_seo.mjs新設=dist成果物を機械検査（title/desc重複・長さ・canonical自己参照・h1・見出し飛び・img alt・OGP実在・内部リンク切れ・孤立ページ・sitemap lastmod・JSON-LD・404）→初回8件検出→2件（title長84件/desc長41件=いずれも記事frontmatter由来）まで削減 ②sitemap lastmod全234URL付与（frontmatter updated由来。ビルド時刻は使わない=毎回全URL更新扱いを避ける） ③トップにWebSite+Organization+SearchAction、一覧11ページにCollectionPage+ItemList、policyにAboutPage、searchにWebPageを追加（JSON-LD欠落21→0） ④記事JSON-LDにarticleSection/keywords/citation（出典28件）/about/publisher logoを追加 ⑤パンくずを画面表示化（Breadcrumb.astro・BreadcrumbListと同一配列を共有）＝topicsの独自crumbも統合 ⑥404.astro新設（noindex・主要導線6+検索CTA）＋nginx.conf.sampleにerror_page 404追加（⚠️VPS実configへの反映は未実施） ⑦titleのサイト名後置を「｜投資の学び舎（まなびや）」→「｜投資の学び舎」に短縮（全角32字超が176→84件）・og:titleはサイト名なしに ⑧一覧6ページのdescriptionを実質化。検証=build緑/validate212緑/Playwright実ブラウザ16通り（PC・SP×8ページ）でJSON-LD全パース成功・consoleエラー0・横スクロール0・h1各1。残=記事title84件/desc41件の短縮は本文編集のためMasaru判断待ち・別コンテキストの/code-review未実施
- 09:04 SEO強化を本番反映完了（a91808d/744fe2d/30051a0）: ①実装をpush→VPS autodeploy反映確認（本番sitemap lastmod 234件・トップWebSite+Organization+SearchAction・記事パンくず表示・用語集titleの短縮を実測確認） ②閾値を実データで校正=当初のdescription全角80字は勘で置いた過大な基準で41件が誤検出だった→PCのSERP表示上限=全角120字へ。titleも全長ではなく「区切り記号より前の主題部」で測る方式に変更（自前で副題を削るとtitle内キーワードを失うため）→84件→4件 ③残4件（用語集の定型末尾）のみ短縮しOGP4枚再生成→check_seo指摘0件 ④VPS nginxにerror_page 404を追加（バックアップ/root/toushi-manabiya.nginx.bak-20260802-090240・nginx -t成功後reload）→存在しないURLで404ステータス＋カスタム404ページ＋noindexを実測確認・既存11URLの回帰も全200 ⑤ops/indexnow.mjs新設（npm run indexnow）で234URL送信HTTP200受理。⚠️教訓=SSHヘルパーでリモートコマンドをbash -cの文字列に埋めるとローカル側で$VARと$()が先に展開される→argvで渡すかbash -sにstdin投入する。残=別コンテキストの/code-review未実施（Masaru起動）
- 10:11 /code-review の結果と対応（5f5901c）: ⚠️レビューが別リポジトリ（~/dev/media-gallery-cms の feat/seo-strengthening）を対象に実行され、今回のtoushi-site変更は未レビュー＝二重レビューは依然未達。原因はセッションのcwdがホーム（非git）だったため。再実行は cd ~/dev/toushi-site してから起動する。指摘7件を自コードに照合した結果、6件は該当なし（NULバイト未使用／パンくずは表示とJSON-LDで同一trail配列を共有／indexnow.mjsは.env非依存でキーファイル名と中身の一致を検査・複数URL可・対象0件は例外停止／check_seo.mjsはdecode()でHTMLエンティティを戻してから幅を測定／canonical集約は未実装）。1件のみ該当＝新設した機械検査がデプロイ経路に入っていない→ops/deploy.shのbuild後に npm run seo を追加。distは入替済みで巻き戻せないためゲートではなく警告（validateは従来どおりbuild前のゲート）。bash -n構文確認・seo単体exit=0確認済
- 11:41 code-review指摘8件を修正し本番反映（eb42dfd）: ⚠️前回の自己照合『NULバイト未使用＝該当なし』は誤りで、ops/check_seo.mjs:23の文字クラス先頭に生0x00が入り、gitが226行の新規スクリプトをBin 0->11544 bytesとして扱っていた＝検査コード自身が差分に1行も出ずレビュー対象から脱落していた。修正=①NUL→エスケープ表記（HEADブロブにNUL残存なし実測）②内部リンク検査がディレクトリ存在だけで合格していた（/og/ /diagrams/がtrue）→index.html→.html→実ファイルの順に変更③リンク・imgの抽出が<main>内限定でヘッダー/フッターが検査外（site-head=offset2655・main=3767〜）→ページ全体へ④content-dates.mjsがreviewed:falseを集計に混入→reviewed:trueのみ⑤横断ページへのsiteNewestフォールバック廃止（234件中158件が同一日付）→lastmod付与217/234・17件は意図的に省略⑥Article.astroのパンくずurlが本番絶対URLでdev/previewから本番へ飛んでいた→サイト相対に統一（JSON-LD側はbreadcrumbJsonLdのabs()で絶対のまま）⑦seo.mjsのisPartOf @id参照が未解決ノード（/learn/に#website実体0件）→最小WebSite実体を併記⑧deploy.shはcronでstdoutが捨てられ警告が消える→ops/seo_report.json出力（gitignore）。あわせてsitemap-lastmod判定を修正（URLごとに省略可なので『一部にしかない』は正常）。検証=注入テストでフッターリンク破損を検出しexit1・復元でexit0／build緑／validate212緑／Playwright16通りALL GREEN／本番実測=パンくず相対・BreadcrumbList絶対・lastmod217・isPartOf実体入り・主要10URL200・存在しないURL404。⚠️教訓2件=(a)監視条件は『変更前に偽』であることを先に確かめる（href=/learn/はナビにも在り即座に一致→反映前にREFLECTEDと誤報告した）(b)deploy.shはgit pullを自身の中で行うため、deploy.sh自体の変更は次回デプロイから有効（今回seo_report.jsonは手動実行で生成・VPSでexit0/235ページ/指摘0件を確認）
## 2026-08-04
- 22:36 SEO強化2（コミット8c50d3d・push済→VPS autodeploy）。①全212記事に前後ナビを機械描画（src/lib/sequence.mjs が読む順序の単一定義・一覧の並びと必ず一致）。本文リンク0本の72記事にも文脈リンクが入る。②robotsメタを全ページ明示（max-image-preview:large / max-snippet:-1 / max-video-preview:-1）。③/search/ をnoindex化しsitemapからも除外（234→233URL）。④robots.txtに Disallow: /pagefind/。⑤learn記事を Article+LearningResource の多重型にし章と レベルを機械可読化。⑥Book構造化データの重複解消（BookCardに集約）＋ASINをisbnとして出していた誤りを修正。⑦check_seo.mjsに不変条件4つ追加（robotsメタ/noindexのsitemap混入/JSON-LD破損/前後ナビ欠落）＝注入テストで全件発火とexit1、復元でexit0を実測。⑧ADR-0005: Lighthouseで唯一落ちていたcolor-contrastを修正（--color-text-muted #71809a→#5b6980 / --color-on-ink-muted #9dbbd4→#a8c6dd / --color-accent #0075c2→#006db5 / .foot-copy・.b-newの直書き）。色相維持・明度のみ・ブランド青のグラデは不変。⑨VPS nginx: gzip_typesがhttpブロックでコメントアウトされておりCSS/JS/SVG/XMLが無圧縮だった→toushi-manabiyaのserverブロック内に限定して追加（他サイト非干渉）。実測 CSS 5025→1638B・SVG 3394→1172B・sitemap 26474→2576B・rss 91769→22149B。バックアップ=/root/toushi-manabiya.nginx.bak-20260804-223515 と -223556。検証: build緑/validate212警告0/SEO検査235ページ指摘0/Playwright16通りALL GREEN/Lighthouse5ページ perf97-100・a11y100・bp100・SEO100・CLS0
- 22:41 本番反映完了（22:40 autodeploy）。本番検証25項目ALL GREEN（前後ナビ/robotsディレクティブ/検索noindex/sitemap233URL・/search/除外/robots.txt/LearningResource/Book重複解消/新トークン反映・旧#71809a残存0/SVG・XMLのgzip/主要8URL 200/存在しないURL404）。IndexNow 233URL HTTP200受理。本番Lighthouse実測（モバイル・記事ページ）= perf98 / a11y100 / best-practices100 / SEO100 / LCP2.4s / CLS0 / TBT20ms。
